---
layout: default
---

# Aprobando Interrupciones

<div class="text-xs mt-4">

```typescript
import { Command } from "@langchain/langgraph";

const config = { configurable: { thread_id: "thread-1" } };

// Primera invocación: se pausa esperando aprobación
let result = await agent.invoke(
  { messages: [{ role: "user", content: "Envía email a juan@example.com" }] },
  config
);

// Aprobar y continuar
result = await agent.invoke(
  new Command({ resume: { decisions: [{ type: "approve" }] } }),
  config
);

// O rechazar con feedback
result = await agent.invoke(
  new Command({ resume: { decisions: [{ type: "reject" }] } }),
  config
);
```

<v-click>

<div class="mt-3 text-xs opacity-80">
💡 Útil para acciones sensibles: emails, pagos, eliminaciones, etc.
</div>

</v-click>

</div>
