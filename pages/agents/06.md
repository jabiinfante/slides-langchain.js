---
layout: default
---

# Ejecutando Tools

<div class="text-xs mt-4">

```typescript
const response = await modelWithTools.invoke("¿Qué tiempo hace en Barcelona?");

if (response.tool_calls?.length > 0) {
  const toolCall = response.tool_calls[0];
  const toolResult = await weatherTool.invoke(toolCall.args);  // Ejecutar tool

  // Enviar resultado de vuelta al LLM para respuesta final
  const finalResponse = await modelWithTools.invoke([
    new HumanMessage("¿Qué tiempo hace en Barcelona?"),
    response,
    new ToolMessage({ tool_call_id: toolCall.id, content: toolResult }),
  ]);
}
```

<v-click>

<div class="mt-4 text-xs opacity-80">
💡 Este flujo manual es lo que <code>createAgent</code> automatiza por ti
</div>

</v-click>

</div>
