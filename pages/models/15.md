---
layout: default
---

# Ejemplo: Agente con Tavily Search

<div class="text-xs mt-6">

<v-click>

```typescript
import { ChatOpenAI } from "@langchain/openai";
import { TavilySearchResults } from "@langchain/community/tools/tavily_search";
import { createReactAgent } from "@langchain/langgraph/prebuilt";

const model = new ChatOpenAI({ model: "gpt-4o-mini" });

const searchTool = new TavilySearchResults({
  apiKey: process.env.TAVILY_API_KEY,
  maxResults: 5,
});

const agent = createReactAgent({
  llm: model,
  tools: [searchTool],
});

const result = await agent.invoke({
  messages: [{ role: "user", content: "¿Cuales son las ultimas noticias sobre IA?" }],
});
```

</v-click>

<v-click>

<div class="mt-4 grid grid-cols-2 gap-4">

<div class="bg-blue-50 dark:bg-blue-900/20 p-3 rounded">

**Tavily** - Optimizado para LLMs
- Extrae contenido relevante
- Filtra anuncios/ruido
- API key gratuita limitada

</div>

<div class="bg-green-50 dark:bg-green-900/20 p-3 rounded">

**Documentacion**
- [langchain.com/integrations/tools](https://docs.langchain.com/oss/javascript/integrations/tools)

</div>

</div>

</v-click>

</div>

