---
layout: default
---

# Indexar en Qdrant

```typescript
import { QdrantVectorStore } from "@langchain/qdrant";
import { OpenAIEmbeddings } from "@langchain/openai";

// Crear vector store desde documentos
const vectorStore = await QdrantVectorStore.fromDocuments(
  chunks,
  new OpenAIEmbeddings(),
  {
    url: "http://localhost:6333",
    collectionName: "manuales_pdf",
  }
);

console.log("✓ Documentos indexados en Qdrant");
```

<v-click>

<div class="mt-6 p-4 bg-green-500/10 rounded">
✨ Con esto ya tenemos nuestros PDFs listos para búsqueda semántica
</div>

</v-click>
