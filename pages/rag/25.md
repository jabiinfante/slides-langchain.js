---
layout: default
---

# RetrievalQA Chain

<v-clicks>

LangChain ofrece una chain predefinida para RAG

```typescript
import { RetrievalQAChain } from "langchain/chains";
import { ChatOpenAI } from "@langchain/openai";

const chain = RetrievalQAChain.fromLLM(
  new ChatOpenAI({ model: "gpt-4o-mini" }),
  vectorStore.asRetriever()
);

const respuesta = await chain.invoke({
  query: "¿Cómo resetear la contraseña?"
});

console.log(respuesta.text);
```

<div class="mt-4 p-4 bg-blue-500/10 rounded">
💡 RetrievalQAChain maneja automáticamente: búsqueda + construcción del prompt + generación
</div>

</v-clicks>
