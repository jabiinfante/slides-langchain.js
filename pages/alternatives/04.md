---
layout: default
---

# LlamaIndex - Ejemplo

```typescript
import {
  Document,
  VectorStoreIndex,
  OpenAI
} from "llamaindex";

// Crear documentos
const document = new Document({ text: "Tu contenido aquí..." });

// Crear índice
const index = await VectorStoreIndex.fromDocuments([document]);

// Query engine
const queryEngine = index.asQueryEngine();

// Consultar
const response = await queryEngine.query({
  query: "¿Qué información contiene el documento?"
});

console.log(response.toString());
```

<v-click>

### Comparación con LangChain

- **LlamaIndex**: Más simple para RAG puro
- **LangChain**: Más flexible para workflows complejos

</v-click>
